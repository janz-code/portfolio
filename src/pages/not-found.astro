---
import PageLayout from "../core/layouts/PageLayout.astro";
const locale = Astro.currentLocale;
const lang = locale ? (locale === "es" ? "es" : "en") : "en";
---

<PageLayout
  path="error"
  lang={lang}
  title={lang === "es" ? "Pagina No Encontrada" : "Page not found"}
/>
