---
interface Props{
  title: string,
  content: string
}
const {title, content} = Astro.props;
---

<div class="grid grid-cols-3 items-center gap-x-4">
  <h4 class="font-semibold font-serif">{title}:</h4>
  <span class="font-light col-span-2 italic">{content}</span>
</div>